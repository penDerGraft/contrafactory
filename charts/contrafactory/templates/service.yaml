apiVersion: v1
kind: Service
metadata:
  name: {{ include "contrafactory.fullname" . }}
  labels:
    {{- include "contrafactory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contrafactory.selectorLabels" . | nindent 4 }}
